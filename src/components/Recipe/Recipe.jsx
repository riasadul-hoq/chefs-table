const Recipe = () => {
  return (
    <div>
      <div className="my-10 text-center">
        <h1 className="text-4xl font-semibold text-default-color mb-8">
          Our Recipes
        </h1>
        <p className="text-base font-normal text-slate-500">
          We use the fresh produce to create each recipe.
        </p>
      </div>
    </div>
  );
};

export default Recipe;
